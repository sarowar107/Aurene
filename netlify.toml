[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20.11.1"
  NPM_FLAGS = "--legacy-peer-deps"
  ENABLE_COREPACK = "true"  # ✅ FIXED

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200