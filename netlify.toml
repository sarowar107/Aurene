[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18.20.4"  # ✅ Use supported Node version
  NPM_FLAGS = "--legacy-peer-deps"
  ENABLE_COREPACK = "true"

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
